# 数据库配置
DATABASE_HOST=postgres
DATABASE_PORT=5432
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=your_database_password
DATABASE_NAME=stock_db
DATABASE_SYNC=false
NODE_ENV=production

# Docker镜像仓库配置（用于docker-compose.prod.yml）
# 根据你的阿里云镜像仓库信息配置：

# 阿里云容器镜像服务配置：
DOCKER_REGISTRY=crpi-qffyuxj0gnzlj59b.cn-hangzhou.personal.cr.aliyuncs.com
DOCKER_NAMESPACE=brooot
DOCKER_REPO_NAME=stock-system

# 注意：你创建的是单个仓库，我们将使用不同的标签来区分前后端镜像
# 前端镜像：stock-system:frontend-latest
# 后端镜像：stock-system:backend-latest

# HTTPS配置
# 服务器端口配置
PORT=80
HTTPS_PORT=443

# 前端与后端通信配置
NEXT_PUBLIC_API_URL=https://your-domain.com
NEXT_PUBLIC_WS_URL=wss://your-domain.com

# SSL证书路径配置（生产环境应使用有效的SSL证书）
SSL_KEY_PATH=/etc/ssl/private/server.key
SSL_CERT_PATH=/etc/ssl/certs/server.crt

# 是否启用HTTPS（生产环境强烈建议启用）
ENABLE_HTTPS=true

# JWT配置
JWT_SECRET=your_production_jwt_secret_here_32_characters_long

# ========== AI 配置（生产） ==========
# 模型供应商：deepseek | ollama
AI_PROVIDER=deepseek
# DeepSeek API Key（仅当 AI_PROVIDER=deepseek 时需要）
DEEPSEEK_API_KEY=
# DeepSeek API Base（可选，默认 https://api.deepseek.com）
DEEPSEEK_API_BASE=https://api.deepseek.com
# 默认模型
AI_MODEL=deepseek-chat

# ========== LangSmith 配置（生产） ==========
# LangChain 追踪功能开关（可选，开启后可观测 AI 对话流程）
LANGSMITH_TRACING=true
# LangSmith API Key（生产环境从安全存储注入）
LANGSMITH_API_KEY=your-api-key
# LangSmith API 端点
LANGSMITH_ENDPOINT=https://api.smith.langchain.com
# LangSmith 项目名称
LANGSMITH_PROJECT=stock-system

# ========== Chroma 向量数据库（生产） ==========
# 生产环境建议内部网络访问，不暴露端口
# 如需外部访问测试，可将 compose 中 chromadb 服务暴露 8000 并把 URL 改为 http://your-domain:8000
CHROMA_URL=http://chromadb:8000
CHROMA_COLLECTION=resume_brooot
