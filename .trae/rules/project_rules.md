如何本地调试开发？(改完代码想启动项目验证怎么办)
- 后端项目会启动在3001端口。先确认如果这个端口已经被占用了，那么可以无需重复启动，会有热更新，直接验证即可。
  - 如果没有启动后端项目，先进入后端子项目中（apps/stock-back-end），然后运行nodemon。
- 前端服务会启动在3000端口，如果已经启动，则无需重复启动，会有热更新。直接验证即可。
  - 如果前端服务没有启动，使用 nx serve stock-fe 启动 前端。
- 包管理工具是 pnpm，需要安装新的包记得使用pnpm。
