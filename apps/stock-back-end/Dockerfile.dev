# 多阶段构建：基础阶段
FROM node:22.17.0-alpine AS base

# 安装系统依赖
RUN apk add --no-cache dumb-init curl git

# 设置工作目录
WORKDIR /app

# 全局安装工具
RUN npm install -g pnpm@9.15.0 nx@20.2.2 nodemon@3.1.7

# 依赖安装阶段
FROM base AS deps

# 复制依赖文件
COPY package.json pnpm-lock.yaml ./
COPY nx.json tsconfig.base.json ./
COPY apps/stock-back-end/project.json ./apps/stock-back-end/
COPY apps/stock-back-end/tsconfig*.json ./apps/stock-back-end/

# 安装依赖
RUN pnpm install --frozen-lockfile

# 开发阶段
FROM deps AS development

# 复制 Prisma schema
COPY prisma ./prisma

# 生成 Prisma Client
RUN npx prisma generate

# 创建必要的目录
RUN mkdir -p apps/stock-back-end/src dist/apps/stock-back-end

# 暴露端口
EXPOSE 3001 9229

# 健康检查
HEALTHCHECK --interval=30s --timeout=10s --start-period=40s --retries=3 \
  CMD curl -f http://localhost:3001/api/health || exit 1

# 设置工作目录到项目根目录下的后端应用
WORKDIR /app/apps/stock-back-end

# 使用 dumb-init 和优化的 nodemon 配置
CMD ["dumb-init", "nodemon", \
     "--watch", "src", \
     "--watch", "../../prisma", \
     "--ignore", "**/*.test.ts", \
     "--ignore", "**/*.spec.ts", \
     "--ignore", "**/node_modules", \
     "--ignore", "**/.nx", \
     "--delay", "3000ms", \
     "--exec", "cd /app && nx build stock-back-end && node dist/apps/stock-back-end/main.js"]
